# Legacy server (VMess inbound). Copy to .env and fill EVERY REPLACE_ME from
# your secret store. Never commit real values.

# Registry namespace for the images (overrides the compose default), e.g. youruser/infra
IMAGE_REPO=youruser/infra

LISTEN_PORT=8744
SERVER_UUID=REPLACE_ME
SERVER_ALTERID=64
